# 墨笺 渲染测试手册

> 测试 LaTeX 数学公式、Mermaid 图表（流程图/序列图/类图/状态图）、表格、代码块的完整渲染能力。

---

## 1. LaTeX 数学公式

### 1.1 行内公式

行内：已知 $Y = \beta_0 + \beta_1 X + \epsilon$，其中 $\epsilon \sim \mathcal{N}(0, \sigma^2)$。

希腊字母：$\alpha, \beta, \gamma, \delta, \theta, \lambda, \mu, \pi, \sigma, \phi, \omega, \Phi, \Delta, \Omega$

### 1.2 块级公式

**线性回归损失函数（MSE）：**

$$J(\theta) = \frac{1}{2m} \sum_{i=1}^{m} \left( h_\theta(x^{(i)}) - y^{(i)} \right)^2$$

**梯度下降更新规则：**

$$\theta_j := \theta_j - \alpha \frac{\partial}{\partial \theta_j} J(\theta)$$

**多元正态分布（概率密度函数）：**

$$f(\mathbf{x}) = \frac{1}{(2\pi)^{d/2} |\boldsymbol{\Sigma}|^{1/2}} \exp \left( -\frac{1}{2} (\mathbf{x} - \boldsymbol{\mu})^T \boldsymbol{\Sigma}^{-1} (\mathbf{x} - \boldsymbol{\mu}) \right)$$

**贝叶斯定理：**

$$P(A|B) = \frac{P(B|A) \cdot P(A)}{P(B)}$$

**矩阵乘法：**

$$\begin{bmatrix} a & b \\ c & d \end{bmatrix} \begin{bmatrix} x \\ y \end{bmatrix} = \begin{bmatrix} ax + by \\ cx + dy \end{bmatrix}$$

**傅里叶变换：**

$$\hat{f}(\xi) = \int_{-\infty}^{\infty} f(x) \ e^{-2\pi i x \xi} \ dx$$

**神经网络前向传播：**

$$a^{(l)} = \sigma\left(W^{(l)} a^{(l-1)} + b^{(l)}\right)$$

### 1.3 常用符号速查

| 类别 | 符号 | LaTeX 代码 |
|:---|:---|:---|
| 求和 | $\sum_{i=1}^{n} x_i$ | `\sum_{i=1}^{n} x_i` |
| 积分 | $\int_{0}^{\infty} f(x)dx$ | `\int_{0}^{\infty} f(x)dx` |
| 偏导 | $\frac{\partial f}{\partial x}$ | `\frac{\partial f}{\partial x}` |
| 极限 | $\lim_{x \to 0} \frac{\sin x}{x}$ | `\lim_{x \to 0} \frac{\sin x}{x}` |
| 根号 | $\sqrt{a^2 + b^2}$ | `\sqrt{a^2 + b^2}` |
| 向量 | $\vec{v}, \mathbf{A}$ | `\vec{v}, \mathbf{A}` |
| 集合 | $x \in \mathbb{R}^n$ | `x \in \mathbb{R}^n` |
| 箭头 | $\Rightarrow, \leftrightarrow$ | `\Rightarrow, \leftrightarrow` |

---

## 2. Mermaid 流程图

### 2.1 CDIO 工程教育闭环

```mermaid
flowchart LR
    C((Conceive<br/>构思)) --> D((Design<br/>设计))
    D --> I((Implement<br/>实施))
    I --> O((Operate<br/>运作))
    O -.->|反馈迭代| C
    
    style C fill:#e3f2fd,stroke:#1565c0
    style D fill:#e8f5e9,stroke:#2e7d32
    style I fill:#fff3e0,stroke:#e65100
    style O fill:#fce4ec,stroke:#c62828
```

### 2.2 用户登录流程

```mermaid
flowchart TD
    Start([开始]) --> Input[输入用户名密码]
    Input --> Check{验证信息}
    Check -- 正确 --> MFA{是否开启双重验证?}
    Check -- 错误 --> Error[显示错误提示]
    Error --> Input
    MFA -- 是 --> SendCode[发送验证码]
    SendCode --> Verify[验证验证码]
    Verify -- 正确 --> Success[登录成功]
    Verify -- 错误 --> SendCode
    MFA -- 否 --> Success
    Success --> End([进入主页])
```

---

## 3. Mermaid 序列图

### 3.1 AI 对话请求流程

```mermaid
sequenceDiagram
    actor User as 用户
    participant App as 客户端
    participant API as API 网关
    participant Model as AI 模型
    
    User->>App: 输入问题
    App->>API: POST /chat (prompt)
    activate API
    API->>API: 鉴权 & 限流检查
    API->>Model: 转发请求
    activate Model
    Model-->>API: 流式返回 tokens
    deactivate Model
    API-->>App: SSE 流式推送
    deactivate API
    App-->>User: 逐字显示回复
```

### 3.2 墨笺文件打开流程

```mermaid
sequenceDiagram
    actor User as 用户
    participant App as 墨笺 App
    participant SAF as 系统文件选择器
    participant WebView as WebView 渲染引擎
    
    User->>App: 点击打开文件
    App->>SAF: 启动文件选择器
    SAF-->>App: 返回文件 URI
    App->>App: readTextFromUri (读取内容)
    App->>App: 修复控制字符 → LaTeX 命令
    App->>WebView: 注入 Markdown + base64
    WebView->>WebView: marked.js 解析 → HTML
    WebView->>WebView: KaTeX 渲染数学公式
    WebView->>WebView: Mermaid 渲染图表
    WebView-->>User: 显示完整渲染结果
```

---

## 4. Mermaid 类图（框架图）

### 4.1 墨笺核心架构

```mermaid
classDiagram
    class MainScreen {
        +markdownContent: String
        +currentFileName: String
        +themeState: Theme
        +openFile()
        +toggleTheme()
        +searchText()
    }
    
    class MarkdownWebView {
        +markdownContent: String
        +onWebViewReady()
        +onSelectionChanged()
        +injectContentBase64()
    }
    
    class ReadingToolbar {
        +onFontSizeChange()
        +onSearchToggle()
        +onBookmark()
        +onThemeToggle()
    }
    
    class TocDrawer {
        +headings: List~Heading~
        +onHeadingClick()
        +extractHeadings()
    }
    
    class TtsEngine {
        +speak()
        +stop()
        +highlightParagraph()
        +isPlaying: Boolean
    }
    
    class SearchBar {
        +query: String
        +findText()
        +nextMatch()
        +prevMatch()
    }
    
    MainScreen --> MarkdownWebView : 包含
    MainScreen --> ReadingToolbar : 包含
    MainScreen --> TocDrawer : 包含
    MainScreen --> TtsEngine : 使用
    MainScreen --> SearchBar : 包含
    MarkdownWebView --> "1" WebView : 封装
```

### 4.2 MVC 架构示意

```mermaid
classDiagram
    class Model {
        +data: State
        +loadFile()
        +saveBookmark()
        +updateSettings()
    }
    class View {
        +render()
        +updateUI()
        +showDialog()
    }
    class Controller {
        +handleUserInput()
        +dispatchAction()
        +updateModel()
    }
    
    Controller --> Model : 更新
    Controller --> View : 通知
    View --> Model : 观察
    View --> Controller : 用户事件
```

---

## 5. Mermaid 状态图

### 5.1 TTS 朗读状态机

```mermaid
stateDiagram-v2
    [*] --> Idle : 初始化
    Idle --> Playing : speak()
    Playing --> Paused : pause()
    Paused --> Playing : resume()
    Playing --> Idle : stop() / 播放完毕
    Paused --> Idle : stop()
    Playing --> Error : TTS 异常
    Error --> Idle : reset()
```

### 5.2 文档阅读状态

```mermaid
stateDiagram-v2
    [*] --> 未打开文件
    未打开文件 --> 浏览中 : 打开文件
    浏览中 --> 搜索中 : 触发搜索
    搜索中 --> 浏览中 : 关闭搜索
    浏览中 --> 朗读中 : 启动 TTS
    朗读中 --> 浏览中 : 停止朗读
    浏览中 --> 翻译中 : 选中翻译
    翻译中 --> 浏览中 : 关闭翻译
    浏览中 --> 全屏模式 : 进入全屏
    全屏模式 --> 浏览中 : 退出全屏
```

---

## 6. 代码块语法高亮

```python
def gradient_descent(X, y, theta, alpha, iterations):
    """线性回归梯度下降"""
    m = len(y)
    cost_history = []
    
    for i in range(iterations):
        # 计算预测值
        h = X @ theta
        
        # 计算梯度
        gradient = (1/m) * X.T @ (h - y)
        
        # 更新参数
        theta = theta - alpha * gradient
        
        # 记录损失
        cost = (1/(2*m)) * np.sum((h - y) ** 2)
        cost_history.append(cost)
    
    return theta, cost_history
```

```kotlin
// 墨笺：控制字符修复
private fun readTextFromUri(context: Context, uri: Uri): String {
    val sb = StringBuilder()
    context.contentResolver.openInputStream(uri)?.use { input ->
        BufferedReader(InputStreamReader(input, Charsets.UTF_8)).use { reader ->
            var line = reader.readLine()
            while (line != null) { sb.appendLine(line); line = reader.readLine() }
        }
    } ?: throw IllegalStateException("无法打开文件: $uri")
    
    // 修复被污染的 LaTeX 转义序列
    return sb.toString()
        .replace("\u000c", "\\f")   // form feed
        .replace("\u0008", "\\b")   // backspace  
        .replace("\u0007", "\\a")   // bell
        .replace("\u0009", "\\t")   // tab
        .replace("\u000d", "\\r")   // cr
}
```

---

## 7. 表格

### 7.1 版本历史

| 版本 | 日期 | 新增功能 | 修复问题 |
|:---|:---|:---|:---|
| v1.0 | 2025-05 | 基础 WebView + Markdown | - |
| v1.1 | 2025-05 | 目录导航、字体缩放 | 中文标题跳转 |
| v1.2 | 2025-05 | TTS 朗读、应用图标 | TTS 初始化 |
| v2.0 | 2025-05 | 主题切换、搜索、书签 | - |
| v2.1 | 2025-05 | AI 翻译 | 书签竞态 |
| v2.2 | 2025-05 | 高亮标注、全屏、选中朗读 | 文件名显示 |
| v2.3 | 2025-05 | 复制选中、高亮刷新 | 文件标题 |
| v2.4 | 2025-05 | **LaTeX 公式修复** | 控制字符还原 |

### 7.2 支持的语法

| 功能 | 引擎 | 状态 |
|:---|:---|:---:|
| Markdown 解析 | marked.js | ✅ |
| LaTeX 行内公式 | KaTeX | ✅ |
| LaTeX 块级公式 | KaTeX | ✅ |
| Mermaid 流程图 | Mermaid | ✅ |
| Mermaid 序列图 | Mermaid | ✅ |
| Mermaid 类图 | Mermaid | ✅ |
| Mermaid 状态图 | Mermaid | ✅ |
| 代码语法高亮 | highlight.js | 🔄 待加 |
| 表格 | marked.js | ✅ |
| 图片 | WebView | ✅ |

---

## 8. 块引用

> **重要提示：** 本文件由 Hermes Agent 生成，用于测试墨笺的完整渲染能力。
> 
> 如果你看到数学公式渲染精美、图表清晰流畅，说明修复成功 🎉
> 
> —— 2025年5月31日

---

*测试文档结束。共包含 14 个 LaTeX 公式、7 个 Mermaid 图表、2 个代码块、2 个表格。*
